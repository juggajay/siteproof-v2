export const APP_NAME = 'SiteProof' as const;
export const APP_VERSION = '2.0.0' as const;

export const API_ROUTES = {
  AUTH: '/api/auth',
  PROJECTS: '/api/projects',
  PROOFS: '/api/proofs',
} as const;