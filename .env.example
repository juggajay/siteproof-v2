# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# WARNING: NEVER commit the service role key to version control!
# This key has admin access and bypasses Row Level Security
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Anthropic AI Configuration
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Security Configuration
CSRF_SECRET=generate-a-random-secret-here

# Rate Limiting (Optional)
INTERNAL_RATE_LIMIT_SECRET=generate-a-random-secret-here

# Environment
NODE_ENV=development

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace all placeholder values with your actual credentials
# 3. NEVER commit .env.local to version control
# 4. Rotate credentials quarterly or immediately if compromised
# 5. See docs/CREDENTIAL_ROTATION.md for detailed procedures

# To generate random secrets, use:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
