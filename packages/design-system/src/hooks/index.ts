// Export from the new use-toast.ts file (with hyphen)
export { useToast, toast } from './use-toast';
export { useTheme, ThemeProvider } from './use-theme';
export type { ThemeProviderProps } from './use-theme';